# Zenith fixtures

`close.auto.json` and `waypoints.json` are copied unchanged from `Horizon-36596/biobuzz` @ `8fa72ba` (`autos/`),
the team's own files, so the AUTO lane plays exactly what the robot plays.
